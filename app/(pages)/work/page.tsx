import { Metadata } from "next";

export const metadata: Metadata = {
  title: "nggiahao.com // work",
  description: "Find a list of all my projects here.",
};

const workPage = () => {
  return (
    <section>
      <h1 className="text-2xl font-bold pb-8">Work</h1>
      <p>wip.</p>
    </section>
  );
};

export default workPage;
