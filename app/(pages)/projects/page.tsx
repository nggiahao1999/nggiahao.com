import { Metadata } from "next";

export const metadata: Metadata = {
  title: "nggiahao.com // projects",
};

const ProjectsPage = () => {
  return (
    <section className="pb-5">
      <h1 className="text-2xl font-bold pb-8">Projects</h1>
      wip.
    </section>
  );
};

export default ProjectsPage;
